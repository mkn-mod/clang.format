#! clean build run

name: clang.format
version: master

parent: base
src: mod.cpp
mode: shared
if_arg:
    win_static: -DYAML_CPP_STATIC_DEFINE
    shared: -DMKN_KUL_SHARED

profile:
- name: base
  dep: mkn.mod

- name: format
  mod: |
    clang.format{init{style: google, paths: .}}
